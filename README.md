# Beebots/ScheduledCacheFlush
This module flushes the Magento 2 fullpage cache on a schedule that you can set via a basic admin config UI.

## Installation
```
composer require "beebots/magento2-scheduled-cache-flush"
```

